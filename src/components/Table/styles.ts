import makeStyles from '@material-ui/core/styles/makeStyles';

export const useStyles = makeStyles({
  root: {
    width: '100%',
  },
  tableWrapper: {
    flexGrow: 1,
    overflow: 'auto',
  },
});
