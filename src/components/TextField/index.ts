import { BaseTextFieldProps as MuiBaseTextFieldProps } from '@material-ui/core/TextField';
import { TextField as MuiTextField } from '@material-ui/core';

export type BaseTextFieldProps = MuiBaseTextFieldProps;
export const TextField = MuiTextField;
