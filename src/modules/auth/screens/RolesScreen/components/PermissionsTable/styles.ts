import makeStyles from '@material-ui/core/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  field: {
    width: 'auto',
    minWidth: 200,
  },
  tableHeader: {
    minWidth: 150,
  },
}));
